log:
  level: INFO
  file: /tmp/calendar_scheduler.log
kafka:
  port: 9092
  host: localhost
  topic: events
storage: sql
db:
  driver: pgx
  dsn: postgres://calendar:calendar@localhost:15432/calendar
timer:
  reminder_events: 5
  old_events: 300